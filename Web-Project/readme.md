### ⚙️ Setup Instructions

1. Copy `config.sample.ini` and rename it to `config.ini`
2. Open `config.ini` and set your **database name** (others can stay default for local setup):

```ini
DB_NAME = "your_database_name"